langgraph>=0.2.50
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
pydantic>=2.6
httpx[socks]>=0.27  # [socks] — для TELEGRAM_PROXY через socks5://
fastapi>=0.115
uvicorn[standard]>=0.30
openpyxl>=3.1
playwright>=1.45
python-dotenv>=1.0
# Telegram-бот ревью тикетов использует httpx (выше) и sqlite3 из stdlib —
# дополнительных зависимостей не требует.
# RAG по базе знаний (DeepSeek + Qdrant)
qdrant-client>=1.9
fastembed>=0.3
beautifulsoup4>=4.12
# Тесты
pytest>=8.0
